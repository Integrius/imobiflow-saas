{
  "buildCommand": "cd apps/web && pnpm install && pnpm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install -g pnpm && pnpm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://imobiflow-saas-1.onrender.com/:path*"
    }
  ]
}
